# Anthropic Claude API (get key from https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here-never-commit-real-key

# API Configuration
ANTHROPIC_API_URL=https://api.anthropic.com/v1/messages
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
ANTHROPIC_MAX_TOKENS=1000
ANTHROPIC_TEMPERATURE=0.7

# Cost Control
AI_REQUESTS_PER_USER_PER_DAY=10
AI_CACHE_TTL_HOURS=24

# Google Calendar OAuth (get credentials from https://console.cloud.google.com)
# Setup instructions:
# 1. Create a new project in Google Cloud Console
# 2. Enable Google Calendar API
# 3. Create OAuth 2.0 credentials (Web application)
# 4. Add authorized redirect URI: http://localhost:8080/api/calendar/oauth/callback
# 5. Copy Client ID and Client Secret here
GOOGLE_CLIENT_ID=123456789-abcdefghijklmnopqrstuvwxyz123456.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_ACTUAL_SECRET_HERE_DO_NOT_COMMIT
GOOGLE_REDIRECT_URI=http://localhost:8080/api/calendar/oauth/callback

# Token encryption (CRITICAL SECURITY!)
# Must be exactly 32 characters for AES-256 encryption
# Generate with: openssl rand -base64 32
# NEVER commit real secret to git!
# CHANGE THIS in production!
TOKEN_ENCRYPTION_SECRET=ThisIsA32CharacterSecretKey12345
